# 項目文件結構說明

## 📂 目錄結構

```
LLMAgent/
├── 📄 主程式
│   ├── bp_react_assistant.py      ⭐ 推薦使用：BP 分析助手（LLM 版本）
│   └── bp_assistant.py            備用：BP 分析助手（正則解析版本）
│
├── 📁 src/                        核心模組
│   ├── agent/                     Agent 模組
│   │   ├── bp_react_agent.py      BP 專用 ReAct Agent
│   │   └── react_agent.py         通用 ReAct Agent 範例
│   │
│   ├── tools/                     工具模組
│   │   ├── bp_predictor.py        BP 預測工具（核心接口）
│   │   ├── hero_name_mapper.py    英雄名稱映射工具
│   │   └── count_letters.py       範例工具
│   │
│   ├── llm_client.py              LLM 客戶端封裝
│   ├── vision/                    圖像處理模組（與 BP 無關）
│   └── rag/                        RAG 模組（與 BP 無關）
│
├── 📁 data/                       數據文件目錄（建議）
│   ├── games.csv                  遊戲數據
│   ├── heroes.csv                 英雄數據
│   └── match_*.csv                原始比賽數據
│
├── 📊 核心文件
│   ├── predict.py                 BP 預測器核心實現
│   ├── preprocessing.py          數據預處理
│   ├── HeroNames.txt              ⭐ 英雄名稱映射表
│   └── bp_predictor.model         ⭐ 訓練好的模型
│
├── 📝 文檔
│   ├── README.md                  項目說明
│   └── PROJECT_STRUCTURE.md       本文件
│
└── 📋 其他
    ├── requirement.txt            依賴包列表
    └── run_demo.py                演示程式
```

## 📄 文件詳細說明

### 🎯 主程式

| 文件 | 用途 | 推薦度 |
|------|------|--------|
| `bp_react_assistant.py` | BP 分析助手主程式（使用 LLM） | ⭐⭐⭐ 推薦 |
| `bp_assistant.py` | BP 分析助手（正則解析，不依賴 LLM） | ⭐ 備用 |

### 🧠 核心模組

#### Agent (`src/agent/`)

| 文件 | 功能 |
|------|------|
| `bp_react_agent.py` | BP 專用 ReAct Agent，使用 LLM 解析自然語言，自動映射英雄名稱 |
| `react_agent.py` | 通用 ReAct Agent 範例 |

#### 工具 (`src/tools/`)

| 文件 | 功能 |
|------|------|
| `bp_predictor.py` | BP 預測工具接口，提供 `predict_winrate()`, `recommend_pick()`, `recommend_ban()` |
| `hero_name_mapper.py` | 英雄名稱映射工具，載入映射表並提供翻譯功能 |
| `count_letters.py` | 範例工具（演示用） |

### 📊 數據文件

| 文件 | 用途 | 重要性 |
|------|------|--------|
| `HeroNames.txt` | 英雄名稱映射表（JSON 格式） | ⭐⭐⭐ 必需 |
| `bp_predictor.model` | 訓練好的 XGBoost 模型 | ⭐⭐⭐ 必需 |
| `games.csv` | 遊戲數據（用於初始化模型） | ⭐⭐ 重要 |
| `heroes.csv` | 英雄數據（用於初始化模型） | ⭐⭐ 重要 |
| `match_*.csv` | 原始比賽數據（用於預處理） | ⭐ 可選 |

### 🔧 核心算法

| 文件 | 功能 |
|------|------|
| `predict.py` | `BPpredictor` 類實現，包含模型訓練、特徵工程、勝率預測 |
| `preprocessing.py` | 數據預處理腳本 |

### 📝 其他

| 文件 | 用途 |
|------|------|
| `README.md` | 項目說明文檔 |
| `PROJECT_STRUCTURE.md` | 項目結構說明（本文件） |
| `requirement.txt` | Python 依賴包列表 |
| `run_demo.py` | 演示程式（與 BP 無關） |

## 🔄 數據流

```
用戶輸入
  ↓
bp_react_assistant.py
  ↓
bp_react_agent.py (LLM 解析)
  ↓
生成 JSON Action
  ↓
bp_predictor.py (執行工具)
  ↓
hero_name_mapper.py (映射名稱)
  ↓
predict.py (BPpredictor.predict_winrate)
  ↓
返回結果
```

## 🗑️ 可清理的文件

以下文件可以考慮移動到 `data/` 或 `archive/` 目錄：

- `match_data.csv`, `match_datashort.csv`, `matchData.csv`, `matchDataShort1.csv`（原始數據）
- `bp_assistant.py`（如果只使用 `bp_react_assistant.py`）
- `run_demo.py`（如果不需要演示功能）

## 📌 重要文件標記

- ⭐⭐⭐ = 必需文件，系統核心依賴
- ⭐⭐ = 重要文件，正常運行需要
- ⭐ = 可選文件，用於特定功能
